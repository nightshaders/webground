mixin js(src)
  script(type='text/javascript' src=src)

mixin style(src)
  link(rel="stylesheet" href=src)

mixin link(src)
  link(rel='stylesheet' href=src)

mixin ngTemplate(id)
  script(type="text/ng-template", id=id)
    block